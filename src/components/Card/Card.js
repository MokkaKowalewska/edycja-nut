import React from "react"
import styled from "styled-components"

const CardWrapper = styled.div`
  height: 200px;
  width: 100px;
  border: 1px solid grey;
`

const Card = ({}) => <CardWrapper></CardWrapper>

Card.propTypes = {}

export default Card
